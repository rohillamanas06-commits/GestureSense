services:
  - type: web
    name: gesturesense-backend
    env: python
    plan: free
    pythonVersion: 3.11.0
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn gesturesense:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PORT
        value: 8000
